# equate variables from setup and login pages to profile pages so they are the same 
# also do an if statement with +1 if follow button is pressed or if a post is added of if user starts following someone else

class Followers
  def initialize(follow_number)
    @button_2 = follow_number
    
    def return_follow_number
    @button_2
    end
    follow_number == true
    current_number == 0
  def followers
    if follow_number == true
      puts "#{current_number + 1}"
    end
  end
end
end
    